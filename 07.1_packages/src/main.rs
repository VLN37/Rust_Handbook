// this is the crate root
// acts as the root module, starting point of the compilation
// package contains a Cargo.toml file, explaining how it compiles

// src/main.rs is the root of a binary crate with the same name as the package

fn main() {
  println!("Packages");
}
